{
  "name": "grsu-ide-backend",
  "private": true,
  "workspaces": [
    "shared/types",
    "services/*"
  ],
  "scripts": {
    "dev:auth": "npm run dev --workspace=services/auth-service",
    "dev:gateway": "npm run dev --workspace=services/gateway",
    "build": "npm run build --workspaces",
    "prisma:generate:all": "npm run db:generate --workspace=services/auth-service && npm run db:generate --workspace=services/fs-service && npm run db:generate --workspace=services/task-service"
  }
}